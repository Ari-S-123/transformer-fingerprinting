# Conclusions

- Historical artifacts evaluated: 8
- Hard-negative targets retrained: 0
- Best baseline historical run: `cnn_baseline_embed128` using `multi5_even` + `ivfflat_nprobe8` with ranking score `0.4009`.
- Highest-floor intervention overall: `baseline` on base run `frozen_mert_extended_embed128` with floor `0.0591`.
